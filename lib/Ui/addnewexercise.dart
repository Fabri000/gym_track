import 'package:flutter/material.dart';

class AddNewExercise extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    OutlinedButton addnew = OutlinedButton(
        onPressed: () {},
        child: Text("Add new Exercise", style: TextStyle(fontSize: 20)));
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
